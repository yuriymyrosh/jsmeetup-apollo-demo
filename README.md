# GraphQL apollo simple demonstration

### Backend:
* To run server `cd backend && npm run serve`

### Client:
* Download schema `cd frontend && npm run schema:download`
* Generate types `npm run types:generate`
